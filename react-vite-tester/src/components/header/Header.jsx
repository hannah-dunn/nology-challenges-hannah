import styles from "./Header.module.scss";

const Header = () => {
  console.log(styles);

  return (
    <header>
      <h1>My first Component</h1>
      <p>Learning React</p>
    </header>
  );
};

export default Header;
